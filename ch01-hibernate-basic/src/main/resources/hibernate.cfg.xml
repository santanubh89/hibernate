<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="connection.url">jdbc:postgresql://localhost:5432/hibernatedb</property>
        <property name="connection.username">postgres</property>
        <property name="connection.password">postgres</property>

        <!-- Database Driver -->
        <property name="connection.driver_class">org.postgresql.Driver</property>

        <!-- JDBC Connection Pool -->
        <property name="connection.pool_size">1</property>

        <!-- SQL Dialect -->
        <property name="dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Disable the second level cache -->
        <property name="hibernate.cache.use_second_level_cache">false</property>
        <property name="hibernate.cache.use_query_cache">false</property>

        <!-- Show SQL Logs -->
        <property name="show_sql">true</property>

        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">update</property>

        <!-- List of Entity classes -->
        <!-- Demo: Column configurations and basic CRUD operations -->
        <mapping class="com.java.hibernate.entity.Employee" />

        <!-- Demo: Primary key type: Sequence -->
        <mapping class="com.java.hibernate.entity.User" />

        <!-- Demo: Primary key type: Generic Generator -->
        <mapping class="com.java.hibernate.entity.LogEvent" />

        <!-- Demo: Primary key type: Composite Key -->
        <mapping class="com.java.hibernate.entity.UserAccount" />
    </session-factory>
</hibernate-configuration>